## API Report File for "@backstage-community/plugin-search-backend-module-typesense"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts
import { BackendFeature } from '@backstage/backend-plugin-api';
import { CollectionFieldSchema } from 'typesense';
import { ConfigurationOptions } from 'typesense';
import { IndexableResultSet } from '@backstage/plugin-search-common';
import { LoggerService } from '@backstage/backend-plugin-api';
import { QueryTranslator } from '@backstage/plugin-search-backend-node';
import { SearchEngine } from '@backstage/plugin-search-backend-node';
import { SearchParams } from 'typesense';
import { SearchQuery } from '@backstage/plugin-search-common';
import { Writable } from 'node:stream';

// @public
const searchModuleTypesenseSearch: BackendFeature;
export default searchModuleTypesenseSearch;
export { searchModuleTypesenseSearch };

// @public
export interface TypesenseEngineOptions {
  apiKey: string;
  clientOptions?: ConfigurationOptions;
  collections?: Record<
    string,
    {
      fields?: CollectionFieldSchema[];
      searchOptions?: SearchParams<any>;
    }
  >;
  logger: LoggerService;
  nodes: Array<{
    host: string;
    port: number;
    protocol: string;
    path?: string;
  }>;
}

// @public
export class TypesenseSearchEngine implements SearchEngine {
  constructor(options: TypesenseEngineOptions);
  getIndexer(type: string): Promise<Writable>;
  query(query: SearchQuery): Promise<IndexableResultSet>;
  setTranslator(_translator: QueryTranslator): void;
}

// (No @packageDocumentation comment for this package)
```
